type Player {
  id: ID! @unique
  nickname: String!
  creattionDate: DateTime
  habitatArray: [Habitat!]!
}

type Alliance {
  id: ID! @unique
  nickname: String!
  playerArray: [Player!]!
  castleScore: Float!
}

type Habitat {
  id: ID! @unique
  mapX: Float!
  mapY: Float!
}
